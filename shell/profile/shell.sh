# Use Vim as the default editor
export EDITOR="vim"

# Display colors in the command line
export CLICOLOR=1

# Add homebrew and heroku to the PATH
export PATH="./.git/safe/../../bin:/usr/local/bin:/usr/local/heroku/bin:/usr/local/blackbox/bin:$PATH"
